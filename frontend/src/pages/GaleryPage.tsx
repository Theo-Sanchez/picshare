import React from "react";

interface Props {};

export const GaleryImage: React.FunctionComponent<Props> = (props) => {
    return  (
        <div>

        </div>
    )
};

export default GaleryImage;