import React from "react";

interface Props {};

export const ConnectPage: React.FunctionComponent<Props> = (props) => {
    return  (
        <div>

        </div>
    )
};

export default ConnectPage;