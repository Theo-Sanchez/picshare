import React from "react";

interface Props {};

export const DashboardPage: React.FunctionComponent<Props> = (props) => {
    return  (
        <div>

        </div>
    )
};

export default DashboardPage;